<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.ounwan.myPage">

	<select id="getPwdById" parameterType="String"
		resultType="String">
		SELECT PASSWORD FROM CLIENTS WHERE CLIENT_ID=#{clientId}
	</select>

	<select id="getUserInfo" parameterType="String"
		resultType="clients">
		SELECT CLIENT_ID, NAME, EMAIL, PHONE, ADDRESS,
		ADDRESS_DETAIL, ZIP_CODE,PROFILE_URL
		FROM CLIENTS WHERE CLIENT_ID = #{clientId}
	</select>

	<update id="modifyPwd" parameterType="clients">
		UPDATE CLIENTS SET PASSWORD = #{password} WHERE CLIENT_ID = #{clientId}
	</update>

	<update id="modifyUserInfo" parameterType="String">
		UPDATE CLIENTS SET PHONE =
		#{phone}, ADDRESS = #{address}, ADDRESS_DETAIL = #{addressDetail},
		ZIP_CODE = #{zipCode} WHERE CLIENT_ID =
		#{clientId}
	</update>
	
	
</mapper>