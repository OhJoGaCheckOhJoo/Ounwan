<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.ounwan.order">
	<insert id="setOrder" parameterType="orders">
		INSERT INTO
		ORDERS(ORDER_NUMBER, CLIENT_ID, GUEST_NUMBER, TRADE_HISTORY_NUMBER, TOTAL_PRICE, ORDER_DATE
		, SHIPPING_ADDRESS, RECEIVER_NAME, RECEIVER_PHONE, PAYMENT_METHOD, TOTAL_QUANTITY)
		VALUES (#{orderNumber}, #{clientId}, #{guestNumber}, #{tradeHistoryNumber}, #{totalPrice}, NOW()
		, #{shippingAddress}, #{receiverName}, #{receiverPhone}, #{paymentMethod}, #{totalQuantity})
	</insert>
    <select id="selectList" parameterType="String" resultType="orders">
    	SELECT * FROM ORDERS WHERE CLIENT_ID = #{clientId}
    	
    </select>
</mapper>