<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.ounwan.coupung">
<select id="getById" parameterType="int"
		resultType="coupung">
		SELECT *
		FROM COUPUNG
		WHERE COUPUNG_NUMBER = #{COUPUNG_NUMBER}
	</select>

	<!-- <select id="getCartById" parameterType="String"
		resultType="carts">
		SELECT NAME, `OPTION`, cart.*
		FROM
		COUPUNG coupung RIGHT JOIN CARTS cart ON
		coupung.COUPUNG_NUMBER = cart.COUPUNG_NUMBER
		WHERE cart.CLIENT_ID = #{CLIENT_ID}
	</select> -->


</mapper>