<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.ounwan.community">

	<select id="AetaList" resultType="aeta">
		select * from CONCERN_BOARDS
		order by BOARD_NUMBER DESC
	</select>

	<select id="AetaSearchAll" parameterType="String" resultType="aeta">
		select * from CONCERN_BOARDS
		where
		title like #{searchWord} or
		CLIENT_ID like #{searchWord}
	</select>

	<select id="AetaSearchTitle" parameterType="String" resultType="aeta">
		select * from CONCERN_BOARDS 
		where TITLE like #{searchWord}
		order by BOARD_NUMBER DESC
	</select>
	<select id="AetaSearchId" parameterType="String" resultType="aeta">
		select * from CONCERN_BOARDS
		where CLIENT_ID like #{searchWord}
		order by BOARD_NUMBER DESC
	</select>
</mapper>

